
Wait until cluster is up, could take a few minutes.

To connect, run:

    kubectl port-forward svc/{{ template "deeb.fullname" . }} 8080:11000 &

Then go to
    https://localhost:8080/openapi-ui/
and enter "https://localhost:8080/openapi-ui/controller.swagger.json" in the "Explore" text input.

